# SVM Modelling
This directory contains the code for training the Support Vector Machine (SVM) model on the provided dataset. The SVM algorithm is a powerful supervised learning method used for classification and regression tasks. In this project, we utilize SVM to classify whether a crop requires irrigation based on various environmental factors.